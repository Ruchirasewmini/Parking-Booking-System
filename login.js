import React from'react';
import{view,StyleSheet} from 'react-native';

const Home =()=> {
    return(
        <Background>
            <Text style={{colour:'white',fontSize:64}}>Let's start</Text>
            <Text style= {{colour:'white',fontSize:64}} >Coding</Text>
          

        </Background>
            
    
    );
}

const styles=StyleSheet.create({})

export default Home;